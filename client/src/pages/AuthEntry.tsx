import { startLogin } from "@/const";
import { ArrowRight, ShieldCheck } from "lucide-react";
import { useLocation } from "wouter";

export default function AuthEntry() {
  const [, setLocation] = useLocation();
  return <main className="swan-auth"><div className="swan-auth-grid"/><section className="relative z-10 grid min-h-screen items-center px-5 py-10 sm:px-10"><div className="mx-auto w-full max-w-5xl"><div className="grid border border-black bg-white lg:grid-cols-[1.15fr_.85fr]"><div className="p-7 sm:p-10"><div className="flex items-center gap-2"><span className="swan-mark"/><span className="text-sm font-bold tracking-[-0.04em]">SwanLab</span></div><p className="mt-16 swan-eyebrow">Secure workspace entry</p><h1 className="mt-5 max-w-xl text-5xl font-semibold leading-[0.92] tracking-[-0.07em] sm:text-7xl">The infrastructure control plane.</h1><p className="mt-7 max-w-md text-sm leading-6 text-black/65">Sign in to open your organization workspace. Product operations are secured on the server and governed by workspace membership roles.</p><button type="button" onClick={() => startLogin()} className="mt-9 flex items-center gap-2 border border-black bg-black px-4 py-3 font-mono text-[10px] uppercase tracking-[0.1em] text-white transition-transform active:scale-[0.97]">Continue securely <ArrowRight className="size-4"/></button><button type="button" onClick={() => setLocation("/app/overview")} className="mt-3 block font-mono text-[10px] uppercase tracking-[0.1em] underline underline-offset-4">View read-only product preview</button></div><aside className="border-t border-black bg-red-600 p-7 text-white lg:border-l lg:border-t-0 sm:p-10"><ShieldCheck className="size-6"/><p className="mt-20 font-mono text-[10px] uppercase tracking-[0.12em] text-white/75">Access model</p><h2 className="mt-3 text-2xl font-semibold leading-tight tracking-[-0.04em]">Identity, membership, project scope.</h2><p className="mt-4 text-sm leading-6 text-white/85">SwanLab protects infrastructure actions with server-side authorization and organization membership checks. Integration credentials stay behind service boundaries.</p></aside></div></div></section></main>;
}
